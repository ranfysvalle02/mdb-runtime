# Example override file for custom configurations
# Copy this to docker-compose.override.yml to customize

services:
  mongodb:
    # Override MongoDB version
    # image: mongo:6.0
    
    # Add custom MongoDB configuration
    # command: mongod --replSet rs0
    
    # Expose additional ports
    # ports:
    #   - "27017:27017"
    #   - "27018:27018"  # Additional port for replica set
    
    # Mount custom MongoDB config
    # volumes:
    #   - ./mongod.conf:/etc/mongod.conf:ro

