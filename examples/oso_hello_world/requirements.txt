# Requirements for OSO Hello World Example
# Install MDB_ENGINE with OSO support: pip install mdb-engine[oso]
# Or install from source: pip install -e ../..[oso]

mdb-engine[oso]>=0.1.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
